📌 Summary: Form Validation and User Feedback in PHP
🎯 Objective

The objective is to validate form data and display appropriate error or success messages based on user input.

1️⃣ Form Submission Verification

Before processing any data, we must check if the form has been submitted.

if (isset($_POST['submit'])) {
  // Form is submitted
}


✔ isset() checks if the submit button exists.

2️⃣ Separation Between HTML and PHP

Best practice is to separate:

HTML → form structure

PHP → data processing and validation

This makes the code cleaner and easier to maintain.

3️⃣ Checking Empty Fields

To ensure required fields are not empty:

if (empty($_POST['username'])) {
  echo "Username is required";
}


✔ empty() detects empty or missing values.

4️⃣ Input Validation with filter_var()

To validate an email address:

if (!filter_var($_POST['email'], FILTER_VALIDATE_EMAIL)) {
  echo "Invalid email address";
}


✔ Ensures correct input format.

5️⃣ Dynamic Display of Messages

Messages are displayed based on validation results:

if (isset($error)) {
  echo "<p style='color:red'>$error</p>";
}

if (isset($success)) {
  echo "<p style='color:green'>Form submitted successfully</p>";
}


✔ Errors are shown when input is invalid
✔ Success messages are shown when input is valid

6️⃣ Conditional Display of Valid Data

Only valid and filled fields are displayed:

if (!empty($_POST['username'])) {
  echo "Username: " . $_POST['username'];
}

🧠 Conclusion
Concept	                  Description
Form submission	          Check if the form is sent
isset()	                  Verifies variable existence
empty()	                  Checks for empty inputs
filter_var()	          Validates user data
Error messages	          Displayed for invalid inputs
Success messages	      Displayed for valid inputs
HTML/PHP separation	      Improves code organization